# Start now — first 90 minutes

## Suyash

1. Send the folder to the team and declare `OP-VARUNA-001`.
2. Assign the seven Codex lanes from the combined plan.
3. Freeze the P0 claim: RGB person candidate + autonomous coverage + metric obstacle safety
   + geotagged alert + offline dashboard + wrong-model HOLD.
4. Create the clean integration worktree and publish its base commit.
5. Open a 30-minute checkpoint board with only: owner, branch, artifact, deadline, blocker.

## Samik

1. Read `03_KAGGLE_DATASET_SHORTLIST.md`; create the clean perception worktree and training
   environment.
2. Start Kaggle Disaster Response YOLO and Smoke-Fire Detection YOLO downloads on separate
   lanes; start official VisDrone and HIT-UAV downloads in parallel.
3. Create `SOURCE.json` for each with slug/owner/version, displayed-terms state, archive
   name and size. Terms may be `TERMS_NOT_DISPLAYED`; begin the private experiment without
   waiting for Suyash or a checksum confirmation.
4. Start HERIDAL only if bandwidth predicts H5 completion.
5. Implement separate converters and render the first 100-image label audit. Launch a
   five-epoch `sar-alert-rgb-k0` smoke job after the Kaggle detector audit passes.
6. Report at minute 60: download %, source/terms states, converted counts,
   duplicate findings and first audit image path.

## Abhijan

1. Copy the scenario-manifest schema into a new scenario branch.
2. Draw the search polygon, five sectors, three person targets, flood polygon, blocked road,
   one navigation obstacle and safe route.
3. Assign actor names and NED coordinates.
4. Deliver the first complete manifest to Pratik by minute 90.

## Pratik

1. Preserve current uncommitted FactoryCity work and create clean world/autonomy worktrees.
2. Make a derived level; do not edit the protected original.
3. Implement/read the scenario manifest—do not hard-code Abhijan's values.
4. Prove the current five vehicles and RGB/depth/pose endpoint still work.
5. In parallel, have the autonomy lane implement deterministic polygon-to-sector and
   sector-to-lawnmower pure functions with tests.

## Ayush

1. Create the perception-support worktree.
2. Build the independent dataset audit and 100-image montage tool.
3. Review the rescue model-registry/observation interfaces.
4. Prepare the current multi-camera app to consume a registry/class map, but do not begin
   a large UI rewrite or duplicate Samik's training.

## Independent verification lane

1. Turn `21_ACCEPTANCE_MATRIX_AND_DEMO.md` into executable test placeholders.
2. Add schema contract tests first.
3. Use overwriteable scratch evidence during development; prepare unique run-ID validation
   only for final H30+ rehearsals.
4. Review, but do not co-author, each lane's output.

## Minute-90 checkpoint

Proceed only if all are true:

- branch/worktree/file ownership is unambiguous;
- P0 claims and event/schema versions are frozen;
- at least one Kaggle detector and one official aerial/thermal source are actively
  downloading with source/version/terms state recorded;
- scenario manifest has coordinates, truth and expected events;
- derived simulator world boots or has a concrete blocker;
- planner pure-function tests have begun; and
- dashboard/event collector interface is compatible with its sample event.

If a condition fails, reassign one Codex lane immediately. Do not expand scope.
