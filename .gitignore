# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
.venv-*/
venv/
env/
*.egg-info/

# SECRETS — generated manifests contain Ed25519 private key seeds.
# Never commit these. Regenerate with node/common.py:generate_manifest().
manifest*.json
nodes/
*.key
*.pem

# Runtime artifacts
results/live_events.jsonl
results/*.jsonl.lock
*.log

# Captured frames — these grow fast. Keep them local.
sim/demo_frames/
sim/captured/
codebase/results/covis_live/

# Pinned model weights are transferred privately and verified at runtime.
codebase/yolov8n.pt

# Paper working notes. Private to Suyash — these discuss what is wrong with an
# unpublished manuscript and must not reach teammates or a public clone.
PAPER_CORRECTIONS.md

# OS
.DS_Store
Thumbs.db
desktop.ini
